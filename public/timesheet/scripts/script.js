/*function plusWeekNumber(ugenummer) {
    ugenummer += 1;
};

let curr = new Date()
function tester() { console.log('tester') };
tester()
let startdate = new Date(curr.getFullYear(), 0, 1);
let days = Math.floor((curr - startdate) / (24 * 60 * 60 * 1000))
let weeknumber = Math.ceil((curr.getDay() + days) / 7)
let week = []
let month = curr.getMonth() + 1
for (let i = 0; i <= 6; i++) {
    let day = curr.getDate() - curr.getDay() + i 
    week.push(day)
}*/